name: GitHub Pages Info

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain Pages publishing mode
        run: echo "GitHub Pages is published from the gh-pages branch root."
